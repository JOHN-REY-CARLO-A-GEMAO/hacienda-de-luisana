/// DEPRECATED — the design system was consolidated into
/// `lib/core/theme/app_theme.dart` (single `AppTheme` source of truth).
///
/// This file is kept as a re-export shim so existing imports
/// (`import '../theme/app_theme.dart'`) keep resolving. New code should
/// import `core/theme/app_theme.dart` directly.
export '../core/theme/app_theme.dart';
